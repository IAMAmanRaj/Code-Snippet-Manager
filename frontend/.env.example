# Frontend development environment

# Browser requests use /api locally; Vite proxies them to this backend.
VITE_API_PROXY_TARGET=http://localhost:5000

# Keep empty in local development so the app uses the Vite proxy.
VITE_API_BASE_URL=
